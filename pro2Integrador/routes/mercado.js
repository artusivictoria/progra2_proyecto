var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', );

router.get('/register', );

router.get('/login', );

router.get('/product', );

module.exports = router;